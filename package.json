{
  "name": "ripple-studio-booking-platform",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "build": "npm -w client run build",
    "dev": "npm -w client run dev"
  }
}
